export const enum HandlerTypes {
  string = `string`,
  number = `number`,
  boolean = `boolean`,
  array = `array`,
  object = `object`,
  api = `api`,
  default = `default`,
}

export const enum RdxGeneratedPrefixes {
  set = `@@rdx/set`,
  get = `@@rdx/get`,
  reset_ = `@@rdx/reset_`,
  set_ = `@@rdx/set_`,
  RESET = `@@rdx/RESET_`,
  SET = `@@rdx/SET_`,
}
